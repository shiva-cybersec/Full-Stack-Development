// ***** Functions *****
// your code that won't run immediately but it will run when you call/ tell it to run also you can tell how many times it should run
// so create one function for doing one work that has many steps to complete that work like eating take plate take out rice in plate then take water and so and so

// 1) Understanding Functions and why it's widely used
['parameters', 'arguments', 'rest parameters', 'hoisting', 'variable hoisting', 'function hoisting']

// 2) parameters - ['required', 'destructed', 'rest' , 'default']

// 3) arguments - ['positional', 'default', 'spread']

// 4) classic function, nested function, function within function, scope chain 

// 5) Immedetaitely Invoked fucntion Expression (IIFE)

// 6) arrow function, fat arrow, anonymous, higher ondragover, callback, first class, pure function, impure function

// 7) scoping ['global', 'function scope']

// 8) closures, scoping rule
